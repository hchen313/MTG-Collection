import React from "react";
//import { Card, CardContent, Typography } from '@mui/material';

interface CardProps {
    name: string; 
    set: string; 
    price: number; 
}

export const Cards: React.FC<CardProps> = ({ name, set, price }) => {
    return (
        <></>
    );
};

